export const getStatuses = (req,res)=>{
   //Get my status plus my contacts's status
   //contacts table and the status table and the logged in user's id
}


export const createStatuses = (req,res) =>{
    //I will need the users id
    //when create a status a timestamp of when the status was 
    // created at will be persisted on the database and a 
    // expiresAt will be generated by adding 24hours to the timestamp
    //Than the system time will be used to delete the status based on the expire time

}

export const deleteStatus=(req,res)=>{
   //the user clicks the button to delete or it simply just expaires
}